package qupath.ext.qpsc.model;
/**
 * ImageTransformModel
 *
 * <p>Model holding the current AffineTransform (QP ↔ stage) and any offsets:
 *   - Stores scaling, flipping, translation between QuPath and microscope coordinates.
 *   - Emits events when updated so the UI can react.
 */

public class ImageTransformModel {
}
